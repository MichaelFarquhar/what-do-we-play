import { Box, Typography } from "@mui/material";

export const Header = () => {
  return (
    <Box component="header">
      <Typography variant="h1" component="h1">
        WDWP
      </Typography>
    </Box>
  );
};
